// 302
const fs = require('fs')

const content = new Date().toString().replace(/T/, ':').replace(/\.\w*/, '')+': WebExec success';

fs.writeFile('poc.txt', content, err => {
  if (err) {
    console.error(err)
    return
  }
  //file written successfully
})
